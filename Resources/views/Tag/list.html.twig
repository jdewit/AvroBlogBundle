{% extends 'AvroBlogBundle::layout.html.twig' %}
{% block center %}
    <div class="header ui-state-active">
        <h1>Tags: </h1>
        <div class="dropDown filter">
            <a href="#" class="dropDownButton">{{ filter }}</a>
            <ul class="dropDownContent border">
                <li><a href=" {{ path('avro_blog_tag_list', {'filter': 'All' }) }}">All</a></li>
                <li><a href="{{ path('avro_blog_tag_list', {'filter': 'Deleted' }) }}">Deleted</a></li>
                <li><a href="#">Search</a></li>
            </ul>
        </div>
        <div id="rightButton">
            <a href="{{ path('avro_blog_tag_new') }}" class="button ui-btn-right">New Tag</a>
        </div>
    </div>
    <div class="content">
        {% include 'AvroBlogBundle:Tag:table.html.twig' %}
    </div>
{% endblock %}
        
